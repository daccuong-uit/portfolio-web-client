# Developer Portfolio Web Client

Independent portfolio repository based on `docs/skills/portfolio/portfolio.md`.

The base includes Home, Selected Work, Experience, Skills, About and Contact sections. Replace placeholder project data and contact links before deployment.

## Commands

- `npm install`
- `npm start`
- `npm run build`
